/**
 * Enum with all user's operations.
 */
enum OperationType {

    LOG_IN_USER,
    GET_USERS,
    GET_ONLINE_USERS,
    GET_USER_BY_ID,
    ADD_USER,
    UPDATE_USER,
    REMOVE_USER_BY_ID,
    GET_UNAUTH_USER_BY_EMAIL

}

export default OperationType