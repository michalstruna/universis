/**
 * List of all urls in app.
 */
enum Urls {

    HOME = '/',
    UNIVERSE = '/universe',
    IDENTITY = '/identity',
    LOGIN = '/login',
    SIGN_UP = '/signup',
    ABOUT = '/about',
    APPROVALS = '/approvals',
    RESET_PASSWORD = '/reset-password',
    USER = '/user'

}

export default Urls