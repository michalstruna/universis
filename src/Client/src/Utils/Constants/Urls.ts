/**
 * List of all urls in app.
 */
enum Urls {

    HOME = '/',
    UNIVERSE = '/universe',
    IDENTITY = '/identity',
    LOGIN = '/login',
    SIGN_UP = '/signup',
    HELP = '/help',
    APPROVALS = '/approvals',
    RESET_PASSWORD = '/reset-password'

}

export default Urls