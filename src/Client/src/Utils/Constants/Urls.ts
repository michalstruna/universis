/**
 * List of all urls in app.
 */
enum Urls {

    HOME = '/',
    UNIVERSE = '/universe',
    IDENTITY = '/identity',
    LOGIN = '/login',
    SIGN_UP = '/signup',
    ABOUT = '/about',
    APPROVALS = '/approval',
    RESET_PASSWORD = '/reset-password',
    ACTIVATE_EMAIL = '/activate-email',
    USER = '/user'

}

export default Urls