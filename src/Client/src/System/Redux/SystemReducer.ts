import ACTION_TYPES from './ActionTypes'

const initialState = {}

export default function (state: any = initialState, action: any) {
    switch (action.type) {
        default:
            return state
    }
}